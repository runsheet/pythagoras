{
  "name": "@runsheet/pythagoras-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Pythagoras AI agent framework - monorepo",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format:write": "turbo run format:write",
    "format:check": "turbo run format:check",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  }
}
